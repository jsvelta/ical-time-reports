CREATE TABLE "ICALTIMEREPORTS"."PERSON"(ID int NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),FIRSTNAME varchar(50) NOT NULL,LASTNAME varchar(100) NOT NULL,USERNAME varchar(25) NOT NULL UNIQUE,PASSWORD varchar(100) NOT NULL,USERROLE varchar(100) NOT NULL)
CREATE UNIQUE INDEX PERSON_ID_IX ON "ICALTIMEREPORTS"."PERSON"(ID)
CREATE UNIQUE INDEX PERSON_USERNAME_IX ON "ICALTIMEREPORTS"."PERSON"(USERNAME)
CREATE TABLE "ICALTIMEREPORTS"."CALENDAR"(ID int NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1), NAME varchar(50), URL varchar(250), PERSON_ID int NOT NULL)
ALTER TABLE "ICALTIMEREPORTS"."CALENDAR" ADD CONSTRAINT FK_CALENDAR_PERSON FOREIGN KEY (PERSON_ID) REFERENCES "ICALTIMEREPORTS"."PERSON"(ID)
CREATE UNIQUE INDEX CALENDAR_ID_IX ON "ICALTIMEREPORTS"."CALENDAR"(ID)
CREATE INDEX CALENDAR_PERSON_IX ON "ICALTIMEREPORTS"."CALENDAR"(PERSON_ID)
